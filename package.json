{
  "name": "fit4110-lab04-nhom2-aivision",
  "version": "1.0.0",
  "description": "FIT4110 Lab 04 - Docker packaging and Newman verification for AI Vision Service",
  "private": true,
  "scripts": {
    "lint:openapi": "spectral lint contracts/*.yaml",
    "test:local": "newman run postman/collections/FIT4110_lab03_ai_vision.postman_collection.json --env-var \"baseUrl=http://localhost:8000\" --env-var \"cameraStreamMockUrl=https://httpbin.org/anything\" -r cli,html,junit --reporter-html-export reports/newman-lab04-local.html --reporter-junit-export reports/newman-lab04-local.xml"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.10.0",
    "@stoplight/spectral-cli": "^6.13.1",
    "newman": "^6.2.1",
    "wait-on": "^8.0.1"
  },
  "dependencies": {
    "newman-reporter-html": "^1.0.5"
  }
}